# Optional — enables live Claude research-mode for low-resource languages.
# Without it, the research engine serves the curated corpus (fully offline).
ANTHROPIC_API_KEY=

# Optional — override the API port (default 8000)
PORT=8000
